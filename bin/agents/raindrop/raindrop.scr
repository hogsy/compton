// Copyright (C) 2019 Mark E Sowden <markelswo@gmail.com>

proc Tick() {
    if(!IsVisible()) {
        Remove($self);
    }

    $position.y += 0.5;
    $position.x += GetWindSpeed($position) / 10.0;
    $angle = -(GetWindSpeed($position) / 100.0);
}
